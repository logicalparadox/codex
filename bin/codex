#!/usr/bin/env node

/**
 * Module dependencies.
 */

var argv = require('optimist').argv
  , Cli = require('..').Cli
  , command = argv._.slice(0);

delete argv._;
delete argv.$0;

Cli.emit(command, argv);